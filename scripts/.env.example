LABEL_STUDIO_URL=http://localhost:8080

# Get from Label Studio UI → Account & Settings → Access Token
LABEL_STUDIO_API_KEY=

# URLs that Label Studio uses to call the models (must be reachable from inside Docker).
# Format: title=url or title=url:interactive — comma-separated for multiple backends.
ML_BACKENDS=SAM3=http://sam3:9090:interactive,YOLO-seg=http://yolo-seg:9090

YOLO_BACKEND_URL=http://localhost:9090